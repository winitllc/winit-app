# Wuzinit Backend

## How to Run, Locally

## How to Deploy

* dev: `npm run dev` (or just `npm run deploy`, as the stage defaults to dev)
* prod: `npm run prod`

## Serverless Framework

Using [serverless framework](https://serverless.com/framework/).
